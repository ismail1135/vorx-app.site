"use strict";
// this file is @generated
exports.__esModule = true;
exports.Environment = void 0;
var environmentIn_1 = require("../models/environmentIn");
var environmentOut_1 = require("../models/environmentOut");
var request_1 = require("../request");
var Environment = /** @class */ (function () {
    function Environment(requestCtx) {
        this.requestCtx = requestCtx;
    }
    /**
     * Download a JSON file containing all org-settings and event types.
     *
     * Note that the schema for [`EnvironmentOut`] is subject to change. The fields
     * herein are provided for convenience but should be treated as JSON blobs.
     */
    Environment.prototype["export"] = function (options) {
        var request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/environment/export");
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        return request.send(this.requestCtx, environmentOut_1.EnvironmentOutSerializer._fromJsonObject);
    };
    /**
     * Import a configuration into the active organization.
     *
     * It doesn't delete anything, only adds / updates what was passed to it.
     *
     * Note that the schema for [`EnvironmentIn`] is subject to change. The fields
     * herein are provided for convenience but should be treated as JSON blobs.
     */
    Environment.prototype["import"] = function (environmentIn, options) {
        var request = new request_1.SvixRequest(request_1.HttpMethod.POST, "/api/v1/environment/import");
        request.setHeaderParam("idempotency-key", options === null || options === void 0 ? void 0 : options.idempotencyKey);
        request.setBody(environmentIn_1.EnvironmentInSerializer._toJsonObject(environmentIn));
        return request.sendNoResponseBody(this.requestCtx);
    };
    return Environment;
}());
exports.Environment = Environment;
